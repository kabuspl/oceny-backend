version: '2'
services:
    node:
        image: "node:latest"
        user: "node"
        working_dir: /home/node/app
        volumes:
            - ./:/home/node/app
        ports:
            - "4080"
        command: "node ."